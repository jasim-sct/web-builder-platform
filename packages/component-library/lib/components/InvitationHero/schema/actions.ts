
import type { ActionPropertySchema } from '../../../schema/types';
export const invitationheroActionsSchema: Record<string, ActionPropertySchema> = {
  primaryAction: { key: 'primaryAction', label: 'Primary Button Action', supportedActions: ['scrollToSection', 'navigate'] }
};

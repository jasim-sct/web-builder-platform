import { standardStyleSchema } from '../../../schema/style';

import type { PropertySchema } from '../../../schema/types';

export const featuresStyleSchema: Record<string, PropertySchema> = {
  ...standardStyleSchema,
};
